<Page x:Class="ProjektWPF.MoviesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:ProjektWPF"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="800"
      Title="MoviesView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <!-- Sekcja dodawania filmu -->
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="MovieTitleTextBox" controls:TextBoxHelper.Watermark="Tytul"  Width="200" />
            <TextBox x:Name="MovieRatingTextBox" controls:TextBoxHelper.Watermark="Ocena"  Width="100" />
            <Button Content="Dodaj film" Click="AddMovieButton_Click" />
            <Button Content="Wroć" Click="BackButton_Click" />
        </StackPanel>

        <!-- Lista filmów -->
        <ListBox x:Name="MoviesListBox" Grid.Row="1" Margin="10"
            SelectionChanged="MoviesListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="20" Margin="5" />
                        <TextBlock Text="{Binding Year}" Margin="5" FontSize="20" />
                        <TextBlock Text="{Binding MyRating, StringFormat='{}{0} / 10'}" Margin="5" FontSize="20"/>

                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Sekcja szczegółów filmu -->
        <StackPanel Grid.Row="1" Grid.Column="1" >
            <Image x:Name="PosterImage" Height="400" Stretch="Uniform" Margin="0,0,0,10" />
            <TextBlock x:Name="TitleDetail" FontSize="26" FontWeight="Bold" Margin="0,0,0,5" Foreground="White"/>
            <TextBlock x:Name="YearDetail" Margin="0,0,0,5" FontSize="15" Foreground="White"/>
            <TextBlock x:Name="GenreDetail" Margin="0,0,0,5" FontSize="15" Foreground="White"/>
            <TextBlock x:Name="RatingDetail" Margin="0,0,0,5" FontSize="15" Foreground="White" />
            <TextBlock x:Name="PlotDetail" TextWrapping="Wrap" Margin="0,0,0,10" FontSize="15" Foreground="White"/>
            <ItemsControl x:Name="ActorsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="White"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
        </StackPanel>
    </Grid>
</Page>
