<Page x:Class="ProjektWPF.ExpensesView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
      xmlns:local="clr-namespace:ProjektWPF"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ExpensesView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="10" Grid.Row="0">
            <TextBox x:Name="ExpenseName" controls:TextBoxHelper.Watermark="Nazwa"   Width="149" />
            <TextBox x:Name="ExpenseValue" controls:TextBoxHelper.Watermark="Wartosc"  Width="126" />
            <TextBox x:Name="ExpenseCategory" controls:TextBoxHelper.Watermark="Rodzaj"  Width="136" />
            <Button Content="Dodaj wydatek" Click="AddExpenseButton_Click" />
            
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="527,9,-455,10">

            <ComboBox x:Name="YearFilter" Width="100" SelectionChanged="FilterExpenses">
                <ComboBoxItem Content="2023"/>
                <ComboBoxItem Content="2024"/>
                <ComboBoxItem Content="2025"/>
            </ComboBox>

            <Button Content="Filtruj" Click="FilterExpenses" />
            <Button Content="Wroć" Click="BackButton_Click" />
        </StackPanel>


       
        <ListBox x:Name="ExpensesListBox" 
             Margin="10"
             Grid.Row="1"
             SelectionChanged="ExpensesListBox_SelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="20" Margin="5" />
                        <TextBlock Text="{Binding Category}" Margin="5" FontSize="20" />
                        <TextBlock Text="{Binding Value, StringFormat='{}{0} zł'}" Margin="5" FontSize="20"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>

        </ListBox>

       
        <TextBlock x:Name="NoExpensesMessage" 
                   Text="Brak wydatków na liście!"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Top"
                   FontSize="18"
                   Foreground="Gray" Margin="0,166,0,0" Grid.Row="1">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>

                        <DataTrigger Binding="{Binding Items.Count, ElementName=ExpensesListBox}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </Grid>
</Page>