<Page x:Class="ProjektWPF.NotebookView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektWPF"
        mc:Ignorable="d"
        Title="NotebookView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            
            <!-- Nowy wiersz na NotePanel -->
        </Grid.RowDefinitions>


        <!-- Pole na tytuł notatki -->
        <TextBox x:Name="TitleBox" FontSize="16" FontWeight="Bold"  Margin="0,0,0,10"/>

        <!-- Pole na treść notatki -->
        
        <RichTextBox x:Name="ContentBox" Grid.Row="1" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Padding="5"/>

        <!-- Przyciski do zapisu i odczytu -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Zapisz" Click="SaveNote_Click" Width="80" Margin="5"/>
            <Button Content="Wczytaj" Click="LoadNote_Click" Width="80" Margin="5"/>
            <Button Content="Wroć" Click="BackButton_Click" />
        </StackPanel>


        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Height="150">
            <StackPanel x:Name="NotePanel" HorizontalAlignment="Stretch" Orientation="Vertical"/>
        </ScrollViewer>

    </Grid>

</Page>
